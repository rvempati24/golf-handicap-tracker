# SQLite database location (relative to the prisma/ directory)
DATABASE_URL="file:./dev.db"

# Anthropic API key — required only for the AI Insights module (milestone 5).
# Get one at https://console.anthropic.com/ . Never commit your real key.
ANTHROPIC_API_KEY=""
